metadatafuncs --- getCounsel last line
--- separating counsels: 
    ,
    and
    with
    him
    (
    )
    for
    the
    plaintiff
    respondent

const regex = |\b(respondent)\b|\b(defendant)\b|\b(appellant)\b

